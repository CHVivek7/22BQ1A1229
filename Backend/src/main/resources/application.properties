# Server Port
server.port=8080

# Spring Data JPA and MySQL Configuration
spring.jpa.hibernate.ddl-auto=update
spring.datasource.url=jdbc:mysql://localhost:3306/url_shortener_db?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=your_mysql_password
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# CORS Configuration (for React frontend)
spring.web.cors.allowed-origins=http://localhost:3000
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=true
spring.web.cors.max-age=3600

# Custom Logging Configuration (Conceptual - replace with actual Logging Middleware details)
# This would typically involve logback-spring.xml or a custom logging appender.
# For the assessment, you'd integrate the specific "Logging Middleware" as provided.
# No System.out.println or standard logging frameworks like SLF4J/Logback/Log4j directly
# for *application logic* output, only for the custom middleware.